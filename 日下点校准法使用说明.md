# 日下点校准法使用说明

## 一、核心原理

**日下点差量校准法** 的核心思路是：**先测现状 → 再算目标 → 最后求差旋转**。

通过比较地球模型的当前日下点经度与真实日下点经度，计算出需要额外旋转的角度，使模型与现实地球完全同步。

```
真实日下点经度 ──┬──> 目标状态
                 │
模型日下点经度 ──┴──> 当前状态
                 │
                 └──> 计算差值 ──> 校准角度
```

## 二、核心函数说明

所有核心函数位于 `src/js/utils.js` 中。

### 1. calculateTrueSubsolarLongitude(utcDate)

计算给定 UTC 时间的真实日下点经度。

- **参数**: `utcDate` - UTC 日期对象
- **返回值**: 日下点经度（度），东经为正，西经为负，范围 -180 到 180
- **计算公式**:
  ```
  平太阳直射经度 = (12 - UTC小时数) × 15
  真实日下点经度 = 平太阳直射经度 + 均时差修正 / 4
  ```

### 2. measureModelSubsolarLongitude(earthMesh, sunWorldPosition)

从地球模型测量当前状态下的日下点经度。

- **参数**:
  - `earthMesh` - 地球模型对象（Mesh）
  - `sunWorldPosition` - 太阳在世界坐标系中的位置（Vector3）
- **返回值**: 模型当前日下点经度（度）
- **实现步骤**:
  1. 强制更新地球的世界矩阵
  2. 获取地球世界坐标位置
  3. 计算从地球指向太阳的方向向量
  4. 提取世界矩阵的旋转部分（法线矩阵）
  5. 将世界方向转换到地球本地坐标系
  6. 从本地方向向量计算经度

### 3. calculateCalibrationAngle(modelLon, trueLon)

计算地球模型需要旋转的校准角度。

- **参数**:
  - `modelLon` - 模型当前日下点经度（度）
  - `trueLon` - 真实日下点经度（度）
- **返回值**: 需要旋转的角度（弧度）
- **计算公式**:
  ```
  经度差 = 目标经度 - 当前经度（归一化到 -180 ~ 180）
  校准角度 = 360° - 经度差（归一化到 0 ~ 360）
  ```

### 4. performSubsolarCalibration(utcDate, earthMesh, sunWorldPosition)

执行完整的日下点差量校准流程。

- **参数**:
  - `utcDate` - 当前 UTC 时间
  - `earthMesh` - 地球模型对象
  - `sunWorldPosition` - 太阳世界坐标位置
- **返回值**: 校准结果对象
  ```javascript
  {
    calibrationAngle: Number,  // 校准角度（弧度）
    debug: {
      utcTime: String,        // UTC时间字符串
      trueSubsolarLon: Number, // 真实日下点经度
      modelSubsolarLon: Number, // 模型日下点经度
      deltaLon: Number,       // 经度差值
      calibrationAngleDeg: Number // 校准角度（度）
    }
  }
  ```

## 三、在 main.js 中的使用流程

### 1. 变量声明（第 351-354 行）

```javascript
let earthInitialRotationOffset = 0; // 初始校准角度
let needsInitialCalibration = true; // 是否需要初始校准
let earthInitialBaseRotation = 0; // 初始时刻的自转基准角度
let simulatedDate = new Date(); // 模拟时间
```

### 2. 初始校准函数（第 145-196 行）

```javascript
function calculateEarthInitialRotation(earthMesh) {
  const date = simulatedDate;
  const sunWorldPosition = new THREE.Vector3(0, 0, 0);

  const calibrationResult = performSubsolarCalibration(
    date,
    earthMesh,
    sunWorldPosition
  );

  console.log("======== 日下点差量校准法 ========");
  console.log("UTC时间:", calibrationResult.debug.utcTime);
  console.log(
    "真实日下点经度（目标）:",
    calibrationResult.debug.trueSubsolarLon.toFixed(2) + "°"
  );
  console.log(
    "模型日下点经度（现状）:",
    calibrationResult.debug.modelSubsolarLon.toFixed(2) + "°"
  );
  console.log(
    "经度差值:",
    calibrationResult.debug.deltaLonDeg.toFixed(2) + "°"
  );
  console.log(
    "校准旋转角度:",
    calibrationResult.debug.calibrationAngleDeg.toFixed(2) + "°"
  );
  console.log("==================================");

  return calibrationResult.calibrationAngle;
}
```

### 3. 动画循环中的校准应用（第 1522-1580 行）

#### 步骤 1：初始化时执行校准

```javascript
if (needsInitialCalibration) {
  // 先应用轴倾斜矩阵
  planet.applyMatrix4(matrix);

  // 执行日下点差量校准
  earthInitialRotationOffset = calculateEarthInitialRotation(planet);

  // 记录初始时刻的自转基准角度
  earthInitialBaseRotation = calculateEarthRotation(simulatedDate);
  needsInitialCalibration = false;

  // 重置并重新构建变换
  planet.matrix.identity();
  planet.rotation.set(0, 0, 0);
  planet.scale.set(1, 1, 1);
  planet.position.set(0, 0, 0);

  // 应用校准角度
  planet.rotateY(earthInitialRotationOffset);
  planet.applyMatrix4(matrix);
  planet.updateMatrixWorld(true);
}
```

#### 步骤 2：每帧计算自转角度

```javascript
// 计算当前自转角度（增量方式）
const currentBaseRotation = calculateEarthRotation(simulatedDate);
const rotationDelta = currentBaseRotation - earthInitialBaseRotation;
const rotationAngle = rotationDelta + earthInitialRotationOffset;

// 应用变换（关键：先绕全局Y轴自转，再应用轴倾斜矩阵）
planet.rotateY(rotationAngle); // 先自转（绕全局Y轴）
planet.applyMatrix4(matrix); // 再轴倾斜
```

## 四、完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    初始化阶段                                │
├─────────────────────────────────────────────────────────────┤
│  1. 创建地球模型，应用轴倾斜矩阵                              │
│  2. 调用 calculateEarthInitialRotation()                    │
│  3. performSubsolarCalibration() 执行以下操作：              │
│     a. 测量模型当前日下点经度（measureModelSubsolarLongitude）│
│     b. 计算真实日下点经度（calculateTrueSubsolarLongitude）  │
│     c. 计算校准角度（calculateCalibrationAngle）             │
│  4. 重置模型矩阵，应用校准角度                               │
│  5. 记录初始自转基准角度（earthInitialBaseRotation）         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    动画循环阶段                              │
├─────────────────────────────────────────────────────────────┤
│  1. 更新模拟时间 simulatedDate                               │
│  2. 计算当前自转角度 calculateEarthRotation(simulatedDate)   │
│  3. 计算角度增量：rotationDelta = 当前 - 初始                │
│  4. 最终角度 = rotationDelta + earthInitialRotationOffset   │
│  5. 应用变换：先自转，再应用轴倾斜矩阵                        │
└─────────────────────────────────────────────────────────────┘
```

## 五、关键要点

| 阶段         | 操作               | 说明                                 |
| ------------ | ------------------ | ------------------------------------ |
| **初始化**   | 测量模型日下点经度 | 获取当前模型状态下的日下点位置       |
| **初始化**   | 计算真实日下点经度 | 基于天文公式计算真实值               |
| **初始化**   | 求差值             | 目标值 - 当前值，归一化到 -180 ~ 180 |
| **初始化**   | 计算校准角度       | `360° - 差值`，归一化到 0 ~ 360      |
| **初始化**   | 应用校准角度       | 绕 Y 轴旋转校准角度，然后应用轴倾斜  |
| **动画循环** | 记录初始基准       | 保存初始时刻的自转角度               |
| **动画循环** | 增量计算           | 当前自转 - 初始自转 + 校准角度       |
| **动画循环** | 应用变换           | 先自转，再轴倾斜                     |

## 六、调试输出示例

```
======== 日下点差量校准法 ========
UTC时间: 2024-01-15T12:30:45.000Z
真实日下点经度（目标）: 15.23°
模型日下点经度（现状）: 45.67°
经度差值: -30.44°
校准旋转角度: 30.44°
==================================
地球初始校准完成，偏移角度: 30.44°
```

## 七、坐标系说明

### 地球本地坐标系

与 Three.js SphereGeometry UV 映射一致：

- 本初子午线（0° 经度）在正 X 轴方向 (x>0, z=0)
- 东经 90° 在正 Z 轴方向 (x=0, z>0)
- 西经 90° 在负 Z 轴方向 (x=0, z<0)
- 180° 经度 在负 X 轴方向 (x<0, z=0)

### 经度计算公式

```javascript
// 从方向向量计算经度
longitude = -Math.atan2(z, x) × (180/π)
```

## 八、注意事项

1. **轴倾斜处理**: 校准应在应用轴倾斜矩阵之后进行
2. **增量计算**: 动画中使用增量方式计算自转，避免累积误差
3. **变换顺序**: 必须先自转（绕全局 Y 轴），再应用轴倾斜矩阵
4. **归一化处理**: 所有角度计算后需归一化到合理范围
